/diagnose.result
/cache

# Ignore Python cache files
__pycache__/
*.py[cod]

/dots/.config/quickshell/ii/.qmlls.ini

# exp-update
/.update-lock

# custom os-release
/os-release

# Emacs auto backup file
*~

# NOTE: `/.claude/` is ignored on `main` but tracked on this branch.
# Do not re-add the `/.claude/` ignore here, or personal Claude Code state
# will stop being versioned. If you see `.claude/` appear in `git status`,
# that's intentional on KemonoNecoTweaks.
